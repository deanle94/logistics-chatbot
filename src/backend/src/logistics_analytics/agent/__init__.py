"""AI interpretation layer.

Placeholder for Slice 0. From Slice 2 this holds the LangChain/LangGraph agent that
turns a question into a tool choice plus structured parameters. It never computes a
number and never reaches ``calculator`` or ``data`` - only ``tools``.
"""
